import { CustumpipePipe } from './custumpipe.pipe';

describe('CustumpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustumpipePipe();
    expect(pipe).toBeTruthy();
  });
});
